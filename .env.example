# Backend port (default 3001 so Store frontend can use 3000)
PORT=3001
MONGODB_URI=mongodb://localhost:27017/store
# Frontend origin(s) for CORS, comma-separated. Use both in dev so localhost and 127.0.0.1 work.
CORS_ORIGIN=http://localhost:3000,http://127.0.0.1:3000
# JWT signing secret for auth tokens (set a strong value in production)
JWT_SECRET=your-secret-key-change-in-production
